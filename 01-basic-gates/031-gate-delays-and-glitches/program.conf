# This program's whole point is gate propagation delay (`#` values on
# primitive instances), which is a simulation/timing concept, not
# synthesizable RTL (see CLAUDE.md §4's stated exception for programs that
# teach delay modelling). Verified by simulation only.
SYNTH=no
SYNTH_NOTE=teaches gate delays and hazards with explicit # delays, which are not synthesizable RTL by repository convention
LINT_FLAGS=--timing
