# Verilator reports UNOPTFLAT for bit-to-bit chains inside one vector
# (bin[i] depends on bin[i+1], carry[i+1] on carry[i]). This is a
# simulation-performance note, not a combinational loop; see README section 12.
LINT_FLAGS=-Wno-UNOPTFLAT
