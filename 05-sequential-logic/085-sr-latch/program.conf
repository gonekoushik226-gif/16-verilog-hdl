SYNTH=no
SYNTH_NOTE=intentional combinational feedback loop (cross-coupled NOR gates) -- Yosys 'check -assert' flags this as a "logic loop" problem, which is exactly the SR latch's gate-level topology, not a design defect. See README.md section 13.
